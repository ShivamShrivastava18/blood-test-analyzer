## Requirements.txt
- Multiple conflicts in requirements.txt
- Fixed by initially removing the version parameters for all packages except crewai and crewai-tools
- Then create a new requirements.txt using pip freeze command

## Tools.py
1: PDFLoader is not deprecated and no longer works in newer version of langchain_community.document_loaders
- Fixed by using PyMuPDFLoader

2:Incorrect SerperDevTool import
- Correct code: from crewai_tools import SerperDevTool
- https://docs.crewai.com/en/tools/search-research/serperdevtool

3: Incorrect custom tool creation
- Creating tools in subclasses requires using BaseTool instead of Tool method.
- I went with the BaseTool method to fix this. Alternatively, you can use the tool decorator @tool. 
- This approach allows you to define the tool’s attributes and functionality directly within a function.
- https://docs.crewai.com/en/learn/create-custom-tools

## Agents.py
1: Incorrect Agent import
- Correct code: from crewai import Agent
- https://docs.crewai.com/en/concepts/agents

2: llm not defined
- I have used ollama to run local Gemma:2b 

3: Invalid tool assignment 
- tool=[BloodTestReportTool().read_data_tool]	
- CrewAI tools must be passed as class instance, not function
- tool (singular) is not valid
- Replaced with tools=[BloodTestReportTool()]
- https://docs.crewai.com/en/concepts/agents

## Task.py
1: Agent=Doctor is used for all tasks
- Fixed by assigning correct agents for each task
2: Invalid tool assignment 
- tool=[BloodTestReportTool().read_data_tool]	
- CrewAI tools must be passed as class instance, not function
- tool (singular) is not valid
- Replaced with tools=[BloodTestReportTool()]
- https://docs.crewai.com/en/concepts/agents


## Main.py
1: Runs on a single agent [doctor] and skips [verifier, nutritionist, exercise_specialist]
- Fixed by importing and assigning correct agents.
2: result = medical_crew.kickoff({'query': query}), Missing input file path
- Fixed by adding file_path to inputs as input path is required to load the blood test report for agent to work on.
